// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"github.com/sparklee/abc-api/internal/biz"
	"github.com/sparklee/abc-api/internal/conf"
	"github.com/sparklee/abc-api/internal/data"
	"github.com/sparklee/abc-api/internal/server"
	"github.com/sparklee/abc-api/internal/service"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData, logger)
	if err != nil {
		return nil, nil, err
	}
	greeterRepo := data.NewGreeterRepo(dataData, logger)
	greeterUsecase := biz.NewGreeterUsecase(greeterRepo, logger)
	greeterService := service.NewGreeterService(greeterUsecase)
	wordRepo := data.NewWordRepo(dataData, logger)
	wordUsecase := biz.NewWordUseCase(wordRepo, logger)
	wordService := service.NewWordService(wordUsecase, logger)
	grpcServer := server.NewGRPCServer(confServer, greeterService, wordService, logger)
	httpServer := server.NewHTTPServer(confServer, greeterService, wordService, logger)
	app := newApp(logger, grpcServer, httpServer)
	return app, func() {
		cleanup()
	}, nil
}
